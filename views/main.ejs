<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title id="titulo">SnapMap ~ <%= title %></title>
    <link rel="stylesheet" id="themeStyles">
    <link rel="icon" type="image/x-icon" id="icono"/>
    <!-- Precargar el script de Google Maps -->
    <link rel="preconnect" href="https://maps.googleapis.com">
    <link rel="preconnect" href="https://maps.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
</head>

<body>
    <div class="app-container">
        <!-- Sidebar -->
        <aside class="sidebar">
            <div class="sidebar-header">
                <a id="btn-logo" class="logo-container">
                    <img alt="Project logo" class="logo" id="logo">
                </a>
                <h1 class="project-title">SnapMap</h1>
            </div>

            <nav class="sidebar-nav">
                <div class="nav-section">
                    <h3>Modo de Visualización</h3>
                    <button id="realTimeBtn" class="nav-button active">
                        <span class="icon">📍</span>
                        Tiempo Real
                    </button>
                    <button id="historicalBtn" class="nav-button">
                        <span class="icon">📅</span>
                        Histórico
                    </button>
                </div>

                <div class="nav-section">
                    <h3>Información</h3>
                    <div class="info-panel">
                        <div class="info-item">
                            <span class="label">Latitud:</span>
                            <span id="latitud" class="value">N/A</span>
                        </div>
                        <div class="info-item">
                            <span class="label">Longitud:</span>
                            <span id="longitud" class="value">N/A</span>
                        </div>
                        <div class="info-item">
                            <span class="label">Fecha:</span>
                            <span id="fecha" class="value">N/A</span>
                        </div>
                        <div class="info-item">
                            <span class="label">Hora:</span>
                            <span id="tiempo" class="value">N/A</span>
                        </div>
                    </div>
                </div>

                <!-- Historical Controls -->
                <div id="historicalControls" class="nav-section" style="display: none;">
                    <h3>Filtros Históricos</h3>
                    <div class="filter-form">
                        <div class="form-group">
                            <label for="startDate">Fecha Inicial</label>
                            <input type="datetime-local" id="startDate" class="form-input">
                        </div>
                        <div class="form-group">
                            <label for="endDate">Fecha Final</label>
                            <input type="datetime-local" id="endDate" class="form-input">
                        </div>
                        
                        <div class="point-selection">
                            <div class="checkbox-group">
                                <input type="checkbox" id="enablePointSelection">
                                <label for="enablePointSelection">Seleccionar punto</label>
                            </div>
                            
                            <div class="coordinates-input">
                                <div class="input-group">
                                    <label>Latitud</label>
                                    <input type="text" id="selectedLat" disabled>
                                </div>
                                <div class="input-group">
                                    <label>Longitud</label>
                                    <input type="text" id="selectedLng" disabled>
                                </div>
                            </div>
                            
                            <div class="input-group">
                                <label>Radio (metros)</label>
                                <input type="number" id="searchRadius" min="10" max="1000" value="50">
                            </div>
                        </div>
                    </div>
                </div>

                <div class="nav-actions">
                    <button id="seguirBtn" class="action-button primary">
                        <span class="icon">👁️</span>
                        Seguir Taxi
                    </button>
                    <button id="loadHistory" class="action-button secondary" style="display: none;">
                        <span class="icon">📊</span>
                        Cargar Trayectoria
                    </button>
                </div>
            </nav>

            <div class="sidebar-footer">
                <a id="about" class="about-link">Acerca de Nosotros</a>
            </div>
        </aside>

        <!-- Main Content -->
        <main class="main-content">
            <!-- Map Container -->
            <div class="map-container">
                <div id="realTimeMap" class="map active">Cargando mapa...</div>
                <div id="historicalMap" class="map">Seleccione un rango de fechas y haga clic en "Cargar Trayectoria"</div>
                
                <!-- Loading Overlay -->
                <div class="loading-overlay" style="display: none;">
                    <div class="loading-spinner"></div>
                    <span class="loading-text">Cargando datos...</span>
                </div>

                <!-- Timeline Controls -->
                <div id="timelineControls" class="timeline-controls" style="display: none;">
                    <div class="timeline-header">
                        <span id="currentTimeInfo">--:--:--</span>
                        <span id="distanceInfo">Distancia: -- m</span>
                    </div>
                    <input type="range" id="timelineSlider" min="0" max="100" value="0" class="timeline-slider">
                </div>
            </div>
        </main>
    </div>

    <!-- Error Messages -->
    <div id="errorContainer" class="error-container" style="display: none;">
        <div class="error-message">
            <span class="error-icon">⚠️</span>
            <span class="error-text"></span>
        </div>
    </div>

    <script>
        const isTesting = window.location.pathname.startsWith('/test');
        document.getElementById('themeStyles').href = isTesting
            ? '/test/styles/main.css'
            : '/styles/main.css';

        document.getElementById('btn-logo').href = isTesting
            ? '/test/'
            : '/';
        const logo = document.getElementById('logo');
        logo.src = isTesting
            ? '/test/images/logo.png'
            : '/images/logo.png';

        const icono = document.getElementById('icono');
        icono.href = isTesting
            ? '/test/images/favicon.ico'
            : '/images/favicon.ico';        

        document.getElementById('about').href = isTesting
            ? '/test/aboutUs'
            : '/aboutUs';
    </script>
    <script src="js/timeline-animation.js"></script>
    <script defer src="js/main.js"></script>
</body>
</html>